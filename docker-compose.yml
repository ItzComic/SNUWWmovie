services:
  supernatural-movie-site:
    image: nginx:alpine
    container_name: supernatural-movie-site
    restart: unless-stopped
    ports:
      - "8088:80"
    volumes:
      - ./:/usr/share/nginx/html
